# arduino-project
# Arduino I2C LCD Display (0x27)

This project demonstrates how to interface a 16x2 LCD (I2C PCF8574) with an Arduino UNO using a clean modular structure with separate `.h` and `.cpp` files.

The LCD uses the I2C address 0x27, and the project includes a custom delay wrapper to support both .ino and .cpp integration.


